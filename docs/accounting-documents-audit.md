## Confirmed code issue

| Document | Area | Current source | Status |
| --- | --- | --- | --- |
| Табель питания | Подпись составителя | `.xlsm` template text, раньше не зависела от аккаунта | Исправлено: подпись теперь берётся из текущего пользователя, формат `Фамилия И. О.` |
| Ведомость стоимости питания | Подпись составителя | `.xlsm` template text, раньше не зависела от аккаунта | Исправлено: подпись теперь берётся из текущего пользователя, формат `Фамилия И. О.` |
| Расчёт стоимости питания | Подпись составителя | `.xlsm` template text, раньше не зависела от аккаунта | Исправлено: подпись теперь берётся из текущего пользователя, формат `Фамилия И. О.` |
| Ведомость стоимости питания | `Дата составления` | backend payload, раньше подставлялся конец периода | Исправлено: теперь ставится фактическая дата генерации документа |
| Все три формы | Ширины колонок в preview/xlsx | раньше часть колонок «плыла» из-за неявных/неодинаковых ширин | Исправлено: добавлены явные width-контракты для ведомости/расчёта и фиксированная ширина day-slot для табеля |

Дополнительно подтверждено:
- Preview и `xlsx` используют один workbook pipeline, поэтому подмена подписи и даты теперь синхронна в обоих форматах.
- Источник подписи составителя убран из `accounting_documents.metadata`; статический `accountant_name` больше не влияет на бухгалтерские формы.
- Добавлены browser-smoke тесты (Playwright) по трём формам: сравнивают фактические ширины колонок в DOM preview с ширинами `xlsx`.

## Template issue

| Document | Header / requisites | Signatures | Source | Note |
| --- | --- | --- | --- | --- |
| Табель питания | Название формы, реквизиты учреждения, структура шапки | `Бухгалтер ...` вшит в ячейку шаблона | mixed: код + `.xlsm` | Для составителя добавлен явный binding по ячейке шаблона; остальные подписи остаются шаблонными |
| Ведомость стоимости питания | `Форма 389`, `по ОКУД`, `по ОКПО`, `по ОКЕИ`, строка учреждения | `Составил`, `Проверил`, верхний блок подписей | преимущественно `.xlsm` | Верхний служебный блок скрывается кодом, но реквизиты и проверяющий остаются шаблонными |
| Расчёт стоимости питания | Заголовок, категория, таблица расчёта | `Ведущий бухгалтер ...` вшит в ячейку шаблона | mixed: код + `.xlsm` | Составитель теперь подменяется кодом; дневной норматив по-прежнему берётся из ячейки шаблона |

Шаблонные риски:
- Бухгалтерские формы всё ещё привязаны к шаблону `Питание_2025.xlsm`.
- Для `Расчёта стоимости питания` дневной норматив берётся из `.xlsm`, а не из конфигурации или БД.

## Needs local form verification

| Document | Field | Current visible value / behavior | Source | Why verification is needed |
| --- | --- | --- | --- | --- |
| Ведомость стоимости питания | `Форма 389` | Видимый текст шаблона | `.xlsm` | Возможна привязка к локально утверждённому бланку, менять автоматически нельзя |
| Ведомость стоимости питания | `по ОКУД 0504403` | Видимый текст шаблона | `.xlsm` | Требуется сверка с вашим локальным образцом и нормативным основанием |
| Ведомость стоимости питания | `по ОКЕИ 0383/383` | Видимый текст шаблона | `.xlsm` | Нужно подтвердить, что значение и формат записи соответствуют утверждённой форме |
| Ведомость стоимости питания | Источник финансирования | Шаблонный текст | `.xlsm` | Бизнес-значение зависит от локального бланка и учётной практики |
| Все три формы | Подписанты кроме составителя | `Проверил`, главный бухгалтер, директор | `.xlsm` | В текущей задаче не менялись; нужно подтвердить актуальность состава подписей |

Рабочие внешние ориентиры для последующей сверки:
- [Приказ Минфина РФ N 52н, унифицированные формы](https://www.consultant.ru/document/cons_doc_LAW_7445/a1171507f8a574a72d6eadb3354b7bdcc466d4d4/)
- [Архивная форма 389 в приказе Минфина РФ N 107н](https://sudact.ru/law/prikaz-minfina-rf-ot-30121999-n-107n/instruktsiia-po-bukhgalterskomu-uchetu-v/prilozhenie-n-1/platezhnaia-vedomost-na-vydachu-avansa/)
